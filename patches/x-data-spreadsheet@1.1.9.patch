diff --git a/src/component/editor.js b/src/component/editor.js
index 616824fc6810a9b539bcfac8e47ca54464bfe7fa..9d4b1a129e54dc729851d419b1108e37a4ae4012 100644
--- a/src/component/editor.js
+++ b/src/component/editor.js
@@ -41,7 +41,7 @@ function insertText({ target }, itxt) {
   target.setSelectionRange(selectionEnd + 1, selectionEnd + 1);
 
   this.inputText = ntxt;
-  this.textlineEl.html(ntxt);
+  this.textlineEl.el.textContent = ntxt;
   resetTextareaSize.call(this);
 }
 
@@ -77,7 +77,7 @@ function inputEventHandler(evt) {
           suggest.hide();
         }
       }
-      textlineEl.html(v);
+      textlineEl.el.textContent = v;
       resetTextareaSize.call(this);
       this.change('input', v);
     } else {
@@ -99,7 +99,7 @@ function inputEventHandler(evt) {
         suggest.hide();
       }
     }
-    textlineEl.html(v);
+    textlineEl.el.textContent = v;
     resetTextareaSize.call(this);
     this.change('input', v);
   }
@@ -119,7 +119,7 @@ function setText(text, position) {
   textEl.el.blur();
 
   textEl.val(text);
-  textlineEl.html(text);
+  textlineEl.el.textContent = text;
   setTextareaRange.call(this, position);
 }
 
@@ -211,7 +211,7 @@ export default class Editor {
     this.inputText = '';
     this.el.hide();
     this.textEl.val('');
-    this.textlineEl.html('');
+    this.textlineEl.el.textContent = '';
     resetSuggestItems.call(this);
     this.datepicker.hide();
   }
